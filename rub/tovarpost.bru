meta {
  name: tovarpost
  type: http
  seq: 9
}

post {
  url: {{base_url}}/items/:id/bids
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {
    "amount": 80000
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
